package com.zuk.conference;


import com.zuk.conference.model.Participant;
import org.junit.Test;
import org.springframework.web.client.RestTemplate;

import static org.junit.Assert.*;

public class TestController {
   /* @Test
    public void testParticicpantInfoResponse() {
        RestTemplate restTemplate = new RestTemplate();
        Participant remoteParticipant = restTemplate.getForObject("http://localhost:8080/login?&login=admin&password=admin", Participant.class);

        assertTrue(remoteParticipant.equals(59));
    }
*/
    //Sorry my tests, but I will take care of you a little later
}
